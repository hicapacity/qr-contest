<% content_for :head do %>
  <%= javascript_include_tag "qr_code_badge" %>
  <script>
    window.badges = <%= raw(@badges.map{|b| {:url => badge_url(b.slug), :img => image_path(b.slug + '.png')}}.to_json) %>;
  </script>
<% end %>

<%= render :partial => 'badges/descriptions/' + @badges[0].slug %>